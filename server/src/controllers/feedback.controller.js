import Feedback from "../models/feedback.model";
import Supplier from "../models/supplier.model";

export const leaveFeedback = async(req,res)=>{
    try{

    }catch(error){
        const {supplierId ,rating ,comment ,bookingId } = req.body
     }
}